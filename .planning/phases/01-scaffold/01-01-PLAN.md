---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - BIO-01
  - BIO-02
  - BIO-03
  - SOCIAL-01
  - SOCIAL-02
  - NOTES-01
  - NOTES-02
  - VISUAL-01
  - VISUAL-03
  - VISUAL-04
  - VISUAL-05

must_haves:
  truths:
    - "Visitor reads the exact locked bio text immediately below where the animated header will appear"
    - "Both current roles (Revcloud/Alyson AI and Forengers) are clearly named in the bio section"
    - "Three badge-style social links for website, LinkedIn, and email are present and point to the correct URLs"
    - "The DS/ML notes callout is visually distinct (badge link + description line, no section heading) and sits above the social footer"
    - "Every major section is separated by a horizontal rule and wrapped in a centered div"
    - "Zero style= attributes appear anywhere in the file"
    - "A deterministic Phase 2 header placeholder comment exists at the top"
  artifacts:
    - path: "README.md"
      provides: "Full static content skeleton for the GitHub profile"
      min_lines: 50
      contains: "<!-- PHASE-2-HEADER -->"
    - path: "README.md"
      provides: "Exact locked bio text"
      contains: "Data scientist by title, engineer by habit."
    - path: "README.md"
      provides: "Social badge links"
      contains: "for-the-badge"
  key_links:
    - from: "README.md social badges"
      to: "adityajoshi.in"
      via: "shields.io badge href"
      pattern: "adityajoshi\\.in"
    - from: "README.md notes badge"
      to: "joshiaditya0511/data-science-notes"
      via: "shields.io badge href"
      pattern: "data-science-notes"
    - from: "README.md"
      to: "Phase 2 header insertion point"
      via: "placeholder comment"
      pattern: "PHASE-2-HEADER"
---

<objective>
Create the complete static README.md skeleton for the GitHub profile. This is the foundation all subsequent phases build on.

Purpose: Phase 1 delivers the full visible structure — bio, role callout, notes callout, social footer, and all visual baseline rules (centering, HR dividers, no style= attributes). Phase 2 will insert the animated header at the placeholder; Phase 3 will append project cards between the notes callout and social footer area.

Output: README.md at repo root with all Phase 1 content complete, verified against the locked decisions D-01 through D-11.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffold/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the README.md static skeleton</name>
  <files>README.md</files>

  <read_first>
    - `.planning/phases/01-scaffold/01-CONTEXT.md` — locked decisions D-01 through D-11 (bio text, role names, link targets, visual rules); especially D-07 (notes callout is secondary — badge + description line, NOT a section heading)
    - `.planning/STATE.md` § Critical Technical Notes — sanitizer rules (no style=), badge style split (for-the-badge vs flat-square), typing SVG endpoint note
    - `.planning/REQUIREMENTS.md` — Phase 1 requirement text for BIO-01..03, SOCIAL-01..02, NOTES-01..02, VISUAL-01,03,04,05
  </read_first>

  <action>
Create `README.md` at the repository root. The file must contain exactly the structure below, preserving all HTML attributes, badge URLs, placeholder comments, and copy verbatim.

---

## EXACT STRUCTURE TO PRODUCE

```
<!-- PHASE-2-HEADER -->

<div align="center">

**Data scientist by title, engineer by habit. I build ML pipelines, ship full-stack products, and head the tech team at a climate NGO.**

🧑‍💻 **Data Scientist** at [Revcloud (Alyson AI)](https://revcloud.com) · **Technical Head** at [Forengers](https://forengers.org)

</div>

---

<div align="center">

[![DS/ML Notes](https://img.shields.io/badge/DS%2FML%20Notes-data--science--notes-58A6FF?style=for-the-badge&logo=github&logoColor=white)](https://github.com/joshiaditya0511/data-science-notes)

My running collection of data science and machine learning notes — algorithms, intuitions, and implementation patterns.

</div>

---

<div align="center">

[![Website](https://img.shields.io/badge/Website-adityajoshi.in-58A6FF?style=for-the-badge&logoColor=white)](https://adityajoshi.in)
[![LinkedIn](https://img.shields.io/badge/LinkedIn-joshiaditya0511-58A6FF?style=for-the-badge&logo=linkedin&logoColor=white)](https://linkedin.com/in/joshiaditya0511)
[![Email](https://img.shields.io/badge/Email-contact%40adityajoshi.in-58A6FF?style=for-the-badge&logo=gmail&logoColor=white)](mailto:contact@adityajoshi.in)

</div>
```

---

## RULES TO ENFORCE

- `align=` attribute on `<div>` only — ZERO `style=` attributes anywhere (per D-10, VISUAL-04).
- Badge style: `for-the-badge` for all standalone badge rows (per D-04, SOCIAL-02).
- All badge colors: `58A6FF` (without `#` prefix, as shields.io URL param) — per D-11.
- Horizontal rule `---` between every major section — bio block, notes block, social block (per D-09, VISUAL-03).
- The placeholder comment `<!-- PHASE-2-HEADER -->` MUST be the very first line in the file (Phase 2 greps for this exact string).
- Blank line after each `<div align="center">` opening tag and before the closing `</div>` tag — required for GitHub's Markdown renderer to process inline Markdown inside HTML blocks.
- Bio text is locked (D-01): "Data scientist by title, engineer by habit. I build ML pipelines, ship full-stack products, and head the tech team at a climate NGO." — do not paraphrase.
- Role callout (D-03): name exactly "Revcloud (Alyson AI)" and "Forengers" — no abbreviation.
- Notes callout (D-07): badge link + one-line description ONLY — NO section heading (no `### DS/ML Notes` or any `#` heading). The badge label "DS/ML Notes" provides sufficient labeling. A heading would give the notes section equal visual weight to major sections, violating the "secondary treatment" constraint.
- Notes repo URL (D-06): `https://github.com/joshiaditya0511/data-science-notes`.
- Social destinations (D-05): website = `https://adityajoshi.in`, LinkedIn = `https://linkedin.com/in/joshiaditya0511`, email = `mailto:contact@adityajoshi.in`.

## BADGE URL REFERENCE (embed exactly as shown)

Social — Website:
`https://img.shields.io/badge/Website-adityajoshi.in-58A6FF?style=for-the-badge&logoColor=white`

Social — LinkedIn:
`https://img.shields.io/badge/LinkedIn-joshiaditya0511-58A6FF?style=for-the-badge&logo=linkedin&logoColor=white`

Social — Email (@ is URL-encoded as %40):
`https://img.shields.io/badge/Email-contact%40adityajoshi.in-58A6FF?style=for-the-badge&logo=gmail&logoColor=white`

Notes callout badge:
`https://img.shields.io/badge/DS%2FML%20Notes-data--science--notes-58A6FF?style=for-the-badge&logo=github&logoColor=white`

## SECTION ORDER

1. `<!-- PHASE-2-HEADER -->` — placeholder for Phase 2 animated header insertion
2. Blank line
3. `<div align="center">` — bio block: bold bio paragraph, then role callout emoji line
4. `</div>`
5. `---`
6. `<div align="center">` — notes callout: badge link on its own line, then one-line description (NO heading)
7. `</div>`
8. `---`
9. `<div align="center">` — social footer: three badge links on one line
10. `</div>`
  </action>

  <verify>
    <automated>grep -c 'style=' README.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'style=' README.md` returns `0` — VISUAL-04 / D-10 enforced
    - `grep -F '<!-- PHASE-2-HEADER -->' README.md` returns at least 1 match — Phase 2 integration point exists
    - `grep -F 'Data scientist by title, engineer by habit.' README.md` returns at least 1 match — BIO-01 locked copy
    - `grep -F 'Revcloud' README.md` returns at least 1 match — BIO-02 role 1
    - `grep -F 'Forengers' README.md` returns at least 1 match — BIO-02 role 2
    - `grep -F 'data-science-notes' README.md` returns at least 1 match — NOTES-01
    - `grep -c 'for-the-badge' README.md` returns at least `4` — NOTES-02 badge + 3 social badges (D-04, SOCIAL-02)
    - `grep -F 'adityajoshi.in' README.md` returns at least 1 match — SOCIAL-01 website
    - `grep -F 'joshiaditya0511' README.md` returns at least 1 match — SOCIAL-01 LinkedIn
    - `grep -F 'contact%40adityajoshi.in' README.md` returns at least 1 match — SOCIAL-01 email
    - `grep -c '^---$' README.md` returns at least `2` — VISUAL-03 horizontal rules
    - `grep -c '<div align="center">' README.md` returns at least `3` — VISUAL-01 centering on all major sections
    - `grep -c '58A6FF' README.md` returns at least `4` — D-11 accent color on all badges
    - `grep -c '^#' README.md` returns `0` — D-07 enforced: no Markdown headings anywhere; notes callout has badge label only, no `###` heading
  </acceptance_criteria>

  <done>README.md exists at repo root with: the exact locked bio text, both role names (Revcloud/Alyson AI and Forengers), the DS/ML notes badge callout with one-line description and NO section heading, three social badges, HR dividers between every section, all major blocks in centered divs, zero style= attributes, zero Markdown headings, and the PHASE-2-HEADER placeholder as line 1.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all Phase 1 acceptance criteria with grep checks</name>
  <files>README.md</files>

  <read_first>
    - `README.md` — the file just created; read it in full before running any checks to catch obvious structural errors visually first
  </read_first>

  <action>
Run each grep check below in sequence. Report PASS or FAIL for each line. If any check FAILS, fix README.md before marking done.

```bash
# VISUAL-04 / D-10: Zero style= attributes
grep -c 'style=' README.md
# Expected: 0

# Phase 2 integration contract
grep -F '<!-- PHASE-2-HEADER -->' README.md
# Expected: 1 match on line 1

# BIO-01: Exact locked bio copy
grep -F 'Data scientist by title, engineer by habit.' README.md
# Expected: 1 match

# BIO-02: Both roles present
grep -F 'Revcloud' README.md
grep -F 'Forengers' README.md
# Expected: 1 match each

# BIO-03 / VISUAL-01: Bio in centered div
grep -n '<div align="center">' README.md
# Expected: at least 3 occurrences (bio, notes, social)

# NOTES-01: Notes repo linked
grep -F 'data-science-notes' README.md
# Expected: at least 1 match

# D-07: No section heading in notes callout (no Markdown # headings anywhere)
grep -c '^#' README.md
# Expected: 0 — no headings; badge label carries the section label

# NOTES-02 + SOCIAL-02 + D-04: for-the-badge style on all standalone badges
grep -c 'for-the-badge' README.md
# Expected: >= 4 (1 notes badge + 3 social badges)

# SOCIAL-01: All three social destinations present
grep -F 'adityajoshi.in' README.md
grep -F 'joshiaditya0511' README.md
grep -F 'contact%40adityajoshi.in' README.md
# Expected: 1 match each

# VISUAL-03 / D-09: HR dividers between sections
grep -c '^---$' README.md
# Expected: >= 2

# D-11: Accent color 58A6FF on badges
grep -c '58A6FF' README.md
# Expected: >= 4 (one per badge)

# Confirm no style= crept in anywhere (final sweep)
grep -n 'style=' README.md
# Expected: no output (zero matches)
```

If all checks pass, proceed to write the summary. If any check fails, open README.md, fix the specific violation, and rerun only the failing check.
  </action>

  <verify>
    <automated>grep -c 'style=' README.md; echo "style= count (must be 0)"; grep -c 'for-the-badge' README.md; echo "for-the-badge count (must be >= 4)"; grep -c '^#' README.md; echo "heading count (must be 0)"</automated>
  </verify>

  <acceptance_criteria>
    - All grep checks in the action block return expected values with zero failures
    - `grep -c 'style=' README.md` returns `0`
    - `grep -c '^#' README.md` returns `0` — no Markdown headings; D-07 secondary treatment preserved
    - `grep -c 'for-the-badge' README.md` returns at least `4`
    - `grep -c '^---$' README.md` returns at least `2`
    - `grep -c '<div align="center">' README.md` returns at least `3`
    - `grep -c '58A6FF' README.md` returns at least `4`
    - `grep -F '<!-- PHASE-2-HEADER -->' README.md` returns at least 1 match
    - `grep -F 'Data scientist by title, engineer by habit.' README.md` returns at least 1 match
    - `grep -F 'Revcloud' README.md` and `grep -F 'Forengers' README.md` both return at least 1 match
    - `grep -F 'data-science-notes' README.md` returns at least 1 match
  </acceptance_criteria>

  <done>All 11 Phase 1 requirement IDs (BIO-01..03, SOCIAL-01..02, NOTES-01..02, VISUAL-01,03,04,05) confirmed present and correct via automated grep checks. README.md is ready for Phase 2 header insertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shields.io CDN → GitHub renderer | Badge SVGs fetched from shields.io at render time; GitHub renders them in an img tag context |
| GitHub Markdown sanitizer → rendered HTML | All HTML in README.md passes through GitHub's sanitizer before display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | shields.io badge SVG content | accept | shields.io is a widely-trusted CDN; badge content is display-only with no auth or PII beyond email in URL |
| T-01-02 | Denial of Service | shields.io availability | accept | Static fallback text renders if badge SVG fails; profile remains readable |
| T-01-03 | Information Disclosure | email address in badge URL (contact%40adityajoshi.in) | accept | Email is intentionally public contact info; obfuscation via %40 encoding reduces naive scrapers |
| T-01-04 | Tampering | style= injection via future edits | mitigate | VISUAL-04 / D-10 enforced by automated grep check in Task 2: `grep -c 'style=' README.md` must return 0 |
</threat_model>

<verification>
Phase 1 is complete when:
- README.md exists at repo root
- `grep -c 'style=' README.md` returns `0`
- `grep -c '^#' README.md` returns `0` (no Markdown headings — D-07 secondary treatment)
- `grep -F '<!-- PHASE-2-HEADER -->' README.md` matches line 1
- `grep -F 'Data scientist by title, engineer by habit.' README.md` matches
- `grep -F 'Revcloud' README.md` and `grep -F 'Forengers' README.md` both match
- `grep -c 'for-the-badge' README.md` returns >= 4
- `grep -c '^---$' README.md` returns >= 2
- `grep -c '<div align="center">' README.md` returns >= 3
- `grep -F 'data-science-notes' README.md` matches
- `grep -F 'adityajoshi.in' README.md`, `grep -F 'joshiaditya0511' README.md`, `grep -F 'contact%40adityajoshi.in' README.md` all match
</verification>

<success_criteria>
A visitor to github.com/joshiaditya0511 sees:
1. (Placeholder where header will appear — Phase 2 fills this)
2. The exact bio text: "Data scientist by title, engineer by habit. I build ML pipelines, ship full-stack products, and head the tech team at a climate NGO."
3. Both current roles named: Revcloud (Alyson AI) and Forengers
4. A visually secondary DS/ML notes callout — badge link + one description line, no heading — linking to joshiaditya0511/data-science-notes
5. Three social badges (Website, LinkedIn, Email) all in accent color #58A6FF using for-the-badge style
6. Horizontal rules separating each section
7. All content centered
8. No style= attributes and no Markdown headings anywhere — GitHub sanitizer will not strip anything
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md` following the summary template at `@/root/.claude/get-shit-done/templates/summary.md`.
</output>
